name: CI/CD - Docker Build & Deploy (Parallel & Resilient)

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  prepare-disk:
    runs-on: ubuntu-latest
    outputs:
      done: true
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Check disk usage before cleanup
        run: df -h

      - name: Cleanup Docker & temp files
        run: |
          echo "Cleaning Docker system..."
          sudo docker system prune -af --volumes || true
          sudo rm -rf /tmp/* ~/.cache || true
          sudo apt-get clean || true
          echo "Cleaning Docker overlay2 (if necessary)..."
          sudo rm -rf /var/lib/docker/overlay2/* || true
          echo "Disk usage after cleanup:"
          df -h

      - name: Set output
        run: echo "done=true" >> $GITHUB_OUTPUT

  build-backend:
    needs: prepare-disk
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push Backend
        uses: docker/build-push-action@v6
        with:
          context: ./server
          file: ./server/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/wntd-server:latest
            ${{ secrets.DOCKER_USERNAME }}/wntd-server:${{ github.sha }}
          build-args: |
            BUILDKIT_INLINE_CACHE=0

      - name: Prune Docker after backend
        run: docker system prune -af --volumes

  build-frontend:
    needs: prepare-disk
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push Frontend
        uses: docker/build-push-action@v6
        with:
          context: ./client
          file: ./client/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/wntd-client:latest
            ${{ secrets.DOCKER_USERNAME }}/wntd-client:${{ github.sha }}
          build-args: |
            BUILDKIT_INLINE_CACHE=0

      - name: Prune Docker after frontend
        run: docker system prune -af --volumes

  deploy:
    needs: [build-backend, build-frontend]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Start SSH Agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

      - name: Test SSH connection to EC2
        run: |
          ssh -o StrictHostKeyChecking=no -o BatchMode=yes ubuntu@13.61.12.156 "echo 'SSH connection successful'" || {
            echo "ERROR: SSH connection failed. Check EC2_SSH_KEY secret and username."
            exit 1
          }

      - name: Copy deploy script & compose file
        run: |
          scp -o StrictHostKeyChecking=no infra/deploy.sh ubuntu@13.61.12.156:/home/ubuntu/
          scp -o StrictHostKeyChecking=no infra/docker-compose.prod.yml ubuntu@13.61.12.156:/home/ubuntu/

          ssh -o StrictHostKeyChecking=no ubuntu@13.61.12.156 'sudo mv /home/ubuntu/deploy.sh /opt/wntd/ && sudo chmod +x /opt/wntd/deploy.sh'
          ssh -o StrictHostKeyChecking=no ubuntu@13.61.12.156 'sudo mv /home/ubuntu/docker-compose.prod.yml /opt/wntd/'

      - name: Install Docker on EC2 (if missing)
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@13.61.12.156 << 'EOF'
            if ! command -v docker &> /dev/null
            then
              sudo apt-get update
              sudo apt-get install -y ca-certificates curl gnupg lsb-release
              sudo mkdir -p /etc/apt/keyrings
              curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
              echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
              sudo apt-get update
              sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
            fi
            docker --version
          EOF

      - name: Deploy with retry on EC2
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@13.61.12.156 << 'EOF'
            MAX_RETRIES=3
            COUNT=0
            until [ $COUNT -ge $MAX_RETRIES ]
            do
              echo "Attempt $((COUNT+1)) of $MAX_RETRIES..."
              sudo /opt/wntd/deploy.sh && break
              echo "Deploy failed. Cleaning Docker and temp files..."
              docker system prune -af --volumes
              sudo rm -rf /tmp/*
              sudo apt-get clean
              COUNT=$((COUNT+1))
              sleep 5
            done

            if [ $COUNT -eq $MAX_RETRIES ]; then
              echo "ERROR: Deploy failed after $MAX_RETRIES attempts."
              exit 1
            fi
          EOF
